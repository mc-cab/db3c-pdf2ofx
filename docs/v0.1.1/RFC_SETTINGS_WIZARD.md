# RFC: Settings / Profile wizard (PARKED — NOT IN SCOPE v0.1.1)

**Status:** PARKED. Do not implement in v0.1.1.

**Idea:** Manage API KEY / MODEL ID profiles in a local JSON (e.g. named profiles, switch without env vars).

**When to revisit:** After Recovery Mode is stable.
